import React from 'react';

export default function DBEModule({ aircraft, session }) {
  return (
    <div style={{ padding:'20px 0' }}>
      <div className="section-title">DBE / ANAC</div>
      <p style={{ color:'var(--text3)', fontSize:13 }}>
        Módulo de declaração de bens de equipamento (DBE) em breve.
      </p>
    </div>
  );
}
